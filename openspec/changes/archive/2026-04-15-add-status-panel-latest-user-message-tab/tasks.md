## 1. Selector 与预览数据组装

- [x] 1.1 提取独立纯函数，基于当前 thread 的 `ConversationItem[]` 解析最后一条用户消息，并组装文本 + 图片数量占位摘要；输入为线程消息列表，输出为状态面板预览模型；验证：补充纯函数单测，覆盖纯文本、纯图片、文本+图片、无用户消息四种场景。
- [x] 1.2 明确首期媒体兼容策略：以 `ConversationItem.images` 为硬约束实现图片占位，非图片附件不纳入本次验收，并在代码注释/命名中保留后续扩展入口；验证：不新增消息存储字段，不改动历史加载协议。

## 2. StatusPanel Dock 接入

- [x] 2.1 在 `status-panel` 模块内新增独立内容组件，负责渲染 `暂无用户对话`、前 `4` 行预览、展开/收起交互；输入为预览模型，输出为只读面板内容；验证：组件测试覆盖空态、四行截断态、展开态。
- [x] 2.2 扩展 `StatusPanel` 的 Tab 类型与 `dock` 变体渲染，新增 `最新用户对话` Tab，但保持 popover 形态不接入；验证：`StatusPanel.test.tsx` 覆盖多引擎可见、默认不自动选中、收到新用户消息时内容更新但不自动切 Tab、既有 Tab 仍可用。

## 3. 文案、样式与回归治理

- [x] 3.1 新增中英文文案键，包括 Tab 名称、空态、展开/收起文案，并为 `status-panel.css` 增加最小样式支持；验证：本地化快照或文案测试通过，长文本在面板内可读且不压坏布局。
- [x] 3.2 执行最小回归验证，确认新增能力未改变 `任务 / 子代理 / 编辑 / Plan` 的默认访问路径，也未把业务逻辑继续内联到 `app-shell.tsx` 或 `useLayoutNodes.tsx`；验证：受影响测试通过，主集成文件仅保留最小挂载级改动，并补充 thread 切换后预览同步更新的验证。
