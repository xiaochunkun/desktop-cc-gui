## 1. 入口语义增强（P0，前置）

- [x] 1.1 将 `SOLO` 入口升级为 `icon + label + tooltip`（输入：现有 session activity header；输出：`实时跟随` 可见文案与 tooltip；验证：UI 渲染测试 + a11y name 断言通过）。
- [x] 1.2 保持默认非侵入边界（依赖：1.1；输入：入口增强后的交互事件；输出：未显式点击前 follow 不自动开启、editor 不抢焦点；验证：交互单测覆盖“仅曝光不触发”场景）。

## 2. 首次引导与状态持久化（P0，依赖 1.x）

- [x] 2.1 实现 `coach mark` 一次性展示状态存储（输入：`userId + workspaceId + featureKey`；输出：首次展示后持久化已读状态；验证：重复进入不再弹出）。
- [x] 2.2 接入首次进入触发时机（依赖：2.1；输入：首次打开 `session activity` 事件；输出：仅首次出现引导；验证：首次出现、二次不出现、跨 workspace 独立）。

## 3. 情境 toast 与频控状态机（P0，依赖 1.x,2.x）

- [x] 3.1 基于 `file-change` 事件实现 nudge 触发（输入：follow 关闭 + 新 file-change；输出：toast 展示 `开启/稍后`；验证：触发条件单测通过）。
- [x] 3.2 实现轮次频控与“稍后”抑制（依赖：3.1；输入：toast 交互 + turn 边界；输出：同轮次只弹一次、新轮次可再弹；验证：连招 B/C 场景测试通过）。
- [x] 3.3 实现“开启 follow”成功/失败双路径（依赖：3.1；输入：用户点击 `开启`；输出：成功进入 follow，失败可恢复并可重试；验证：连招 F 场景测试通过）。
- [x] 3.4 实现 follow 开启下的重复打开去抖（依赖：3.3；输入：连续 file-change 事件流；输出：按序跟随且无重复打开抖动；验证：连招 D 稳定性测试通过）。

## 4. 埋点与灰度开关（P1，依赖 1.x~3.x）

- [x] 4.1 接入核心埋点事件（输入：入口曝光/点击、toast 曝光/点击、follow 开启；输出：`solo_entry_exposed` 等事件上报；验证：埋点快照测试与字段契约校验通过）。
- [x] 4.2 增加 feature flag 分阶段控制（依赖：4.1；输入：三段 flag 配置；输出：Phase 1/2/3 可独立开关；验证：开关回归测试通过）。

## 5. 回归与执行门禁（P0，收尾）

- [x] 5.1 补齐连招回归用例（输入：A~F 场景矩阵；输出：首次直达/中途补开/稍后再开/关闭后再开/失败重试/连续修改稳定性测试；验证：目标测试套件全绿）。
- [x] 5.2 运行类型检查与构建验证（依赖：5.1；输入：前端改动；输出：无新增类型错误与构建错误；验证：`pnpm tsc --noEmit`、`pnpm run build` 通过）。
- [x] 5.3 执行灰度回退演练（依赖：4.2,5.2；输入：异常阈值触发模拟；输出：可在不改代码情况下回退至 Phase 1 或关闭高层提示；验证：手工回归通过并记录操作步骤）。
