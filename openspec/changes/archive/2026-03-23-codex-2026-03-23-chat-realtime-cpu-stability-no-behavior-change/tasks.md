## 1. Baseline 与边界门禁（P0）

- [x] 1.1 建立多会话实时回放基线夹具（优先级：P0；依赖：无；输入：3-thread 事件序列样本；输出：可重复回放脚本与数据；验证：同一输入重复回放结果一致）。
- [x] 1.2 固化 CPU/帧耗时/事件完整性基线报告（优先级：P0；依赖：1.1；输入：基线回放脚本；输出：5 分钟与 60 分钟基线报告；验证：报告可复现且指标可比）。
- [x] 1.3 落地 Boundary Guard 自动检查（优先级：P0；依赖：1.1；输入：优化前后事件流；输出：行为等价断言脚本；验证：顺序、终态、消息完整性全部通过）。

## 2. 事件摄入与状态收敛优化（P0）

- [x] 2.1 实现 thread-scoped 无损微批处理（优先级：P0；依赖：1.3；输入：高频 delta 事件；输出：批量 dispatch 管线；验证：事件无丢失且顺序保持）。
- [x] 2.2 实现 reducer no-op 短路与引用稳定（优先级：P0；依赖：2.1；输入：reducer 更新路径；输出：无变化场景复用旧引用；验证：no-op 单测与渲染次数下降）。
- [x] 2.3 增加批处理与 no-op feature flag（优先级：P0；依赖：2.1/2.2；输入：开关配置；输出：可独立启停的优化模块；验证：开关切换不影响生命周期语义）。

## 3. 增量派生与渲染去重（P0）

- [x] 3.1 将 thread items 派生改为增量失效（优先级：P0；依赖：2.2；输入：thread revision 与派生逻辑；输出：按变更 thread 重算机制；验证：未变更线程不重算）。
- [x] 3.2 收敛 Messages 中重复 parse/O(n) 计算（优先级：P0；依赖：3.1；输入：消息渲染路径；输出：revision 级复用结果；验证：同 revision 仅计算一次）。
- [x] 3.3 增加增量派生 feature flag 与降级路径（优先级：P0；依赖：3.1；输入：优化模块；输出：独立回退能力；验证：关闭后恢复基线行为且测试通过）。

## 4. Session Activity / Radar 与 Debug 路径优化（P1）

- [x] 4.1 改造 activity/radar 为 changed-thread 增量刷新（优先级：P1；依赖：3.1；输入：会话聚合逻辑；输出：局部重算实现；验证：无重复行且无全量重建）。
- [x] 4.2 收敛 debug 高频序列化开销（优先级：P1；依赖：2.3；输入：debug 事件写入链路；输出：默认轻量路径 + debug 开关重路径；验证：默认模式 CPU 明显下降，debug 语义不丢）。

## 5. 兼容性与稳定性验收（P0）

- [x] 5.1 补齐跨引擎生命周期等价测试（优先级：P0；依赖：2.x/3.x；输入：Codex/Claude/OpenCode 回放样本；输出：等价测试集；验证：跨引擎终态一致且无 stuck processing）。
- [x] 5.2 完成 5 分钟与 60 分钟压测验收（优先级：P0；依赖：4.x；输入：全链路优化版本；输出：优化后性能报告；验证：满足 proposal 的 CPU 降峰与稳定性门槛）。
- [x] 5.3 形成发布/回滚手册（优先级：P0；依赖：5.2；输入：开关与监控项；输出：分层回滚 SOP；验证：演练可在不迁移数据前提下恢复基线路径）。
