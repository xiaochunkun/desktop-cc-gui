## 1. 四按钮二段式框架（P0）

- [x] 1.1 [依赖: 无] [优先级: P0] 抽象通用确认弹窗骨架（输入: 现有 Push/Pull 交互模式；输出: 可复用 confirmation modal
  容器；验证: 可承载标题、说明区、确认/取消动作）。
- [x] 1.2 [依赖: 1.1] [优先级: P0] 新增统一说明区组件（`Intent/Will Happen/Will NOT Happen/Example`）（输入:
  操作语义文案；输出: 结构化说明卡片；验证: 四个字段都可渲染且支持 i18n）。
- [x] 1.3 [依赖: 1.1] [优先级: P0] 定义并落地四动作 icon 映射（输入: Pull/Sync/Fetch/Refresh 语义；输出: 统一 icon
  map；验证: 按钮与弹窗标题 icon 一致，Fetch/Refresh 不重复）。
- [x] 1.4 [依赖: 1.1,1.2] [优先级: P1] 完成弹窗视觉分层规范（输入: 三段式布局要求；输出:
  标题区/说明区/操作区层级样式；验证: 信息层次清晰，风险提示突出）。

## 2. Pull Dialog 扩展（P0）

- [x] 2.1 [依赖: 1.1] [优先级: P0] 新增 Pull 参数配置（remote/target/options/chips）（输入: 现有一键 pull 入口；输出: 参数化
  Pull Dialog；验证: options 选择与 chips 增删正常）。
- [x] 2.2 [依赖: 2.1] [优先级: P0] 保持策略互斥与后端冲突保护（输入: 互斥矩阵；输出: UI disabled + 后端校验；验证:
  非法组合被阻断并提示）。
- [x] 2.3 [依赖: 2.1,1.2] [优先级: P0] 在 Pull Dialog 中加入意图与示例说明（输入: 当前 remote/branch/options；输出: 动态
  Example 文案；验证: 参数变化时 Example 同步更新）。

## 3. Sync/Fetch/Refresh 二段式落地（P0）

- [x] 3.1 [依赖: 1.1,1.2] [优先级: P0] 实现 Sync Dialog（输入: sync 既有语义；输出: 确认后执行 sync；验证: 未确认前不执行，确认后执行
  pull->push）。
- [x] 3.2 [依赖: 3.1] [优先级: P0] 在 Sync Dialog 增加 Preflight 摘要（输入: source/remote/target、ahead-behind、outgoing
  commits；输出: 确认前摘要区；验证: 三类信息首屏可见且随上下文刷新）。
- [x] 3.3 [依赖: 1.1,1.2] [优先级: P0] 实现 Fetch Dialog（输入: fetch 既有语义；输出: 确认后执行 fetch；验证: 未确认前不执行，确认后仅
  fetch 不 merge）。
- [x] 3.4 [依赖: 3.3] [优先级: P1] 在 Fetch Dialog 标注 fetch 作用域（输入: 默认 scope 规则；输出: `all remotes`
  说明；验证: 弹窗示例与执行参数一致）。
- [x] 3.5 [依赖: 1.1,1.2] [优先级: P0] 实现 Refresh Dialog（输入: refreshAll 数据加载链路；输出: 确认后执行
  refreshAll；验证: 未确认前不执行，确认后仅刷新 UI 数据）。

## 4. 快捷键范围声明（P2）

- [x] 4.1 [依赖: 1.1] [优先级: P2] 明确记录当前无 Pull/Sync/Fetch 操作快捷键（输入: settings 与代码现状；输出:
  范围声明；验证: 任务验收与 spec 不再假设已有快捷键）。
- [x] 4.2 [依赖: 4.1] [优先级: P2] 预留未来约束：若新增操作快捷键，必须复用确认弹窗入口（输入: shortcut 设计约束；输出:
  设计备注/实现 guard；验证: 不允许绕过确认直接执行）。

## 5. 参数链路与兼容性（P0）

- [x] 5.1 [依赖: 2.1] [优先级: P0] 补齐 pull payload 映射（输入: Pull Dialog 状态；输出: wrapper payload；验证: 单测覆盖
  remote/branch/options）。
- [x] 5.2 [依赖: 5.1] [优先级: P0] 保留默认兼容行为（输入: 空 options 场景；输出: 与旧语义一致执行路径；验证: 回归测试通过）。

## 6. 测试与质量门禁（P0）

- [x] 6.1 [依赖: 2.3,3.1,3.3,3.5] [优先级: P0] 增加四按钮二段式交互测试（输入: toolbar 点击与弹窗操作；输出:
  测试用例；验证: 取消不执行、确认才执行）。
- [x] 6.2 [依赖: 2.2,5.2] [优先级: P0] 增加 pull 选项映射与冲突校验测试（输入: 多组参数组合；输出: 单元测试；验证:
  合法组合通过、非法组合报错）。
- [x] 6.3 [依赖: 1.3,1.4,3.2,3.4] [优先级: P1] 增加 UI 语义一致性验收测试（输入: icon 与说明区规范；输出:
  快照/交互断言；验证: 四弹窗均包含四段说明、Sync 预览与 Fetch scope 文案正确）。
- [x] 6.4 [依赖: 6.1,6.2,6.3] [优先级: P0] 运行 `npm run typecheck`、`vitest`、`cargo check`（输入: 全量改动；输出:
  门禁结果；验证: 三项通过）。

## 7. 发布前收口（P1）

- [x] 7.1 [依赖: 6.4] [优先级: P1] 对照交互稿执行手工验收（输入: 目标按钮组与弹窗稿；输出: 差异清单；验证: 四按钮均二段式，说明区完整且
  icon 语义正确）。
- [x] 7.2 [依赖: 7.1] [优先级: P1] 追加发布说明（输入: 行为变化点；输出: release notes；验证: 明确记录
  Pull/Sync/Fetch/Refresh 全部改为二段式确认）。
