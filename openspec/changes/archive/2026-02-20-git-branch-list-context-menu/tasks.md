## 1. 菜单交互骨架（P0）

- [x] 1.1 [依赖: 无] 定义 `BranchContextAction` 注册结构与固定分组顺序（输入: proposal 菜单列表；输出: 可渲染 action
  schema；验证: 单测断言 `Checkout` 为第一可执行项）。
- [x] 1.2 [依赖: 1.1] 为分支行接入 `contextmenu` 事件并建立菜单状态容器（输入: branch row 事件；输出:
  可打开/关闭的上下文菜单状态；验证: 右键分支行弹出菜单、`Esc` 可关闭）。
- [x] 1.3 [依赖: 1.2] 保持左键选中链路不变（输入: 左键点击分支行；输出: 仅选中/高亮，不触发菜单动作；验证: 回归测试通过）。

## 2. 菜单动作与安全规则（P0）

- [x] 2.1 [依赖: 1.1] 实现本地/远程/当前分支的动作可见性与禁用规则（输入: branch type + current flag；输出: 动态 action
  matrix；验证: 矩阵测试覆盖当前分支禁用 `Checkout`/`删除`）。
- [x] 2.2 [依赖: 2.1] 将 `checkout/create/rename/delete/merge/rebase/push/update/compare/show-diff`
  映射到现有执行器（输入: menu action；输出: 复用现有 command 调用；验证: 每个动作有可执行单测或集成断言）。
- [x] 2.3 [依赖: 2.2] 对删除、变基、合并等高风险动作补齐确认流（输入: destructive action trigger；输出: 二次确认后执行；验证:
  取消时无副作用、确认时执行成功）。

## 3. 视觉与可访问性（P1）

- [x] 3.1 [依赖: 1.2] 移除分支行尾常驻 checkout 按钮并调整行布局（输入: 现有 branch row UI；输出: 简洁行样式；验证:
  快照对比不再出现行尾 checkout 按钮）。
- [x] 3.2 [依赖: 1.2] 完成菜单定位、分组分隔、禁用态提示样式（输入: pointer 坐标 + action state；输出: 可读菜单 UI；验证:
  手工验证长列表/长分支名场景不遮挡关键内容）。
- [x] 3.3 [依赖: 1.2] 补齐键盘导航与焦点管理（输入: Arrow/Enter/Esc 键；输出: 菜单可键盘操作；验证: 可访问性测试与手工回归通过）。

## 4. 文案与国际化（P1）

- [x] 4.1 [依赖: 2.2] 新增/更新中文菜单文案（输入: proposal 菜单文案；输出: zh 词条完整；验证: UI 文案与图 2 顺序一致）。
- [x] 4.2 [依赖: 4.1] 同步英文词条并确保 key 命名一致（输入: zh 词条；输出: en 对应词条；验证: i18n key 检查无缺失）。

## 5. 测试与发布验证（P0）

- [x] 5.1 [依赖: 2.3,3.3] 新增分支菜单单元测试（输入: action schema + branch fixtures；输出:
  菜单顺序/禁用态/分组断言；验证: 测试全通过）。
- [x] 5.2 [依赖: 5.1] 补齐关键动作回归测试（输入: checkout/push/update/rename/delete 路径；输出: 入口迁移后行为一致；验证:
  现有 Git 测试无回归）。
- [x] 5.3 [依赖: 5.2] 手工验收并记录验证清单（输入: 本地桌面端构建；输出: 验收记录含通过/失败项；验证: 满足 proposal
  验收标准后再合并）。

## 6. 分支差异性能修复（P0）

- [x] 6.1 [依赖: 2.2] 将“显示与工作树的差异”改为轻量文件列表接口（输入: branch；输出: 差异文件名列表；验证: 大变更量场景可秒开列表）。
- [x] 6.2 [依赖: 6.1] 增加单文件详情接口并按需加载 diff（输入: branch + path；输出: 文件级 diff 文本；验证: 点击文件时才发起详情请求）。
- [x] 6.3 [依赖: 6.2] 调整弹窗交互为“左侧文件列表 + 右侧详情区”，补齐 loading/error 状态（输入: 列表与详情状态机；输出: 无卡死
  UI；验证: 手工回归与单测通过）。

## 7. 分支跟踪顶栏增强（P1）

- [x] 7.1 [依赖: 2.1] 在菜单 view-model 中增加 tracking 摘要字段（输入: 分支对象 + upstream 信息；输出:
  `<branch> -> <upstream>` 展示模型；验证: 本地有 upstream/无 upstream/远程分支三类数据断言正确）。
- [x] 7.2 [依赖: 7.1,3.2] 在右键弹窗最上层渲染 tracking 只读条（输入: tracking 摘要；输出: 动作区之前的不可交互顶栏；验证:
  `Checkout` 仍为第一可执行项）。
- [x] 7.3 [依赖: 7.2,4.2] 新增 tracking 占位 i18n 文案（输入: 无 upstream 场景；输出: `(<未设置上游分支>)`
  的中英文文案；验证: i18n key 检查与 UI 快照通过）。
- [x] 7.4 [依赖: 7.2,5.1] 增加菜单顺序与焦点测试（输入: 菜单渲染树；输出: 顶栏存在且不参与 action focus；验证: 键盘首焦点落在
  `Checkout`）。
- [x] 7.5 [依赖: 7.4] 手工验收 tracking 展示（输入: 已设置 upstream 与未设置 upstream 的本地分支；输出: 验收记录；验证: 与图
  2 风格一致且无交互回归）。

## 8. 分支比较动作有效化（P0）

> 说明：第 8 节为第一版“文件级 compare”实现，已可用但不满足最新“IDEA 双向提交集合比较”要求。后续以第 9 节为准。

- [x] 8.1 [依赖: 2.2] 将 `compare-current` 的执行语义从“切换 selectedBranch”升级为“打开 branch compare 结果视图”（输入:
  target/current 分支；输出: compare 状态机入口；验证: 点击 compare 时不再出现无反馈 no-op）。
- [x] 8.2 [依赖: 8.1] 新增 branch-to-branch 文件列表查询接口（输入: target/current；输出: changed files list；验证:
  大分支差异可在可接受时间内返回列表）。
- [x] 8.3 [依赖: 8.2] 新增 branch-to-branch 单文件详情接口（输入: target/current/path；输出: file diff 文本；验证:
  点击文件时按需加载详情）。
- [x] 8.4 [依赖: 8.1,8.2,8.3] 复用并接入 compare 结果视图 UI（输入: compare 状态；输出: 文件列表+详情+loading/error；验证:
  已选中分支场景点击 compare 仍有可见结果）。
- [x] 8.5 [依赖: 8.4,5.1] 增加 compare 专项测试（输入: compare action 触发链路；输出: 调用断言+焦点断言+无 no-op
  断言；验证: 自动化测试通过）。
- [x] 8.6 [依赖: 8.5] 手工验收 compare 可用性（输入: 目标分支已选中/未选中两种路径；输出: 验收记录；验证: 交互一致且符合菜单文案语义）。

## 9. 与分支比较逻辑修订（IDEA 视图对齐）（P0）

- [x] 9.1 [依赖: 8.1] 重定义 compare 业务语义为“双向提交集合比较”（输入: target/current；输出: `target \\ current` 与
  `current \\ target` 视图模型；验证: 不再以文件差异列表作为 compare 主视图）。
- [x] 9.2 [依赖: 9.1] 新增双向提交列表查询接口（输入: target/current；输出: 两个方向的提交列表与基础元数据；验证:
  能正确区分双方独有提交）。
- [x] 9.3 [依赖: 9.2] 接入提交详情加载链路（输入: selected commit sha；输出: 提交摘要 + 变更文件列表 + 文件级 diff
  预览数据；验证:
  选择不同方向提交时详情正确切换）。
- [x] 9.4 [依赖: 9.1,9.2,9.3] 实现 compare workspace 视图（参考 IDEA，上下/左右双列表 + 详情区）（输入: compare state；输出:
  双向提交列表可浏览，点击详情区文件可弹出单文件变化预览；验证: 一侧为空时另一侧可独立浏览）。
- [x] 9.5 [依赖: 9.4,5.1] 增加 compare 逻辑专项测试（输入: 双向提交 fixture；输出: 列表方向断言 + 详情联动断言 + no-op
  防回归；验证: 自动化测试通过）。
- [x] 9.6 [依赖: 9.5] 手工验收 compare 视图一致性（输入: 与 IDEA 对照用例；输出: 验收记录；验证: 语义、布局和交互符合预期）。
