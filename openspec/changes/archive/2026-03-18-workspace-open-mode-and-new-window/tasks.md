## 1. 打开模式分流与入口整合

- [x] 1.1 设计并接入 `OpenMode`（`current-window | new-window`）状态流，统一菜单与侧栏“添加工作区”入口（验证：两个入口触发同一分流逻辑）。
- [x] 1.2 增加“添加工作区”模式选择交互（验证：每次添加工作区均询问打开方式，且无二次记忆弹窗）。
- [x] 1.3 补齐 i18n 文案与可访问性标签（验证：中英文切换下文案完整、无缺失 key）。

## 2. 新建窗口能力与启动链路

- [x] 2.1 新增“新建窗口”菜单项与 `CmdOrCtrl+Shift+N` 快捷键映射（验证：快捷键触发后创建新窗口）。
- [x] 2.2 打通新窗口承载目标路径的启动链路，复用 `open-paths` / pending paths 机制（验证：新窗口启动后自动定位到目标项目）。
- [x] 2.3 实现新窗口启动失败的可恢复提示与回退路径（验证：故障场景下当前窗口保持可操作）。

## 3. 去重与一致性保障

- [x] 3.1 在导入前加入路径规范化去重策略（验证：相同路径重复导入不新增记录，仅激活已有 workspace）。
- [x] 3.2 覆盖 `open-paths` 导入链路的幂等处理（验证：应用启动参数重复传入时无重复 workspace）。
- [x] 3.3 评估并补强多实例并发写 workspace 存储的一致性防护（验证：并发导入场景下 workspace 列表无覆盖丢失）。

## 4. 测试与回归门禁

- [x] 4.1 新增前端单测：打开模式分流、单次询问、去重激活（验证：相关测试稳定通过）。
- [x] 4.2 新增/更新 Tauri 侧测试：菜单快捷键事件、新窗口启动参数、失败回退（验证：命令与事件链路通过）。
- [x] 4.3 执行最小回归：现有“添加工作区到当前窗口”默认行为保持兼容（验证：老路径无行为退化）。
