## 1. 生命周期契约与删除语义收敛（P0）

- [x] 1.1 定义统一删除结果结构与错误码映射（输入：现有 `removeThread` 与后端删除接口；输出：统一类型与错误映射表；验证：typecheck
  通过）
- [x] 1.2 实现跨引擎删除适配层（depends on: 1.1）（输入：统一契约；输出：Claude/Codex/OpenCode 删除适配；验证：单测覆盖成功/失败路径）
- [x] 1.3 批量删除状态机改造（depends on: 1.2）（输入：删除回执；输出：成功移除、失败保留与可重试；验证：组件测试通过）
- [x] 1.4 删除后重启一致性回归测试（depends on: 1.3）（输入：会话样本；输出：重启一致性测试用例；验证：已删会话不回魂）

## 2. OpenCode 入口与排序稳定性（P0）

- [x] 2.1 统一 Workspace Home 与 Composer 引擎可选判定（输入：两处判定逻辑；输出：单一可用性判定来源；验证：两处 UI 一致）
- [x] 2.2 校正首页 OpenCode 新建会话链路（depends on: 2.1）（输入：首页启动流程；输出：进入统一线程生命周期；验证：会话可继续/可删除）
- [x] 2.3 设计 OpenCode 会话排序时间基线与回退顺序（输入：session list 输出；输出：时间解析与回退策略；验证：重启前后排序稳定）
- [x] 2.4 增加时间解析失败可观测（depends on: 2.3）（输入：异常时间样本；输出：诊断日志与安全回退；验证：异常不崩溃且有日志）

## 3. 幕布文案与 i18n 一致性（P1）

- [x] 3.1 PlanPanel 文案 i18n 化（输入：PlanPanel 文案点位；输出：标题/空态/生成态全量 i18n；验证：中英文切换无硬编码）
- [x] 3.2 OpenCode 等待提示 i18n + fallback 统一（输入：Messages 心跳提示逻辑；输出：统一 key 与 fallback；验证：多语言一致）
- [x] 3.3 文案快照回归（depends on: 3.1, 3.2）（输入：多语言场景；输出：快照测试；验证：CI 稳定通过）

## 4. 联调、门禁与回滚预案（P0）

- [x] 4.1 执行跨引擎联调清单（depends on: 1.x, 2.x）（输入：Claude/Codex/OpenCode 场景；输出：联调记录；验证：关键路径全部通过）
- [x] 4.2 执行质量门禁（depends on: 3.3, 4.1）（输入：typecheck、前端测试、后端测试；输出：门禁报告；验证：全部通过）
- [x] 4.3 准备回滚与降级演练（depends on: 4.2）（输入：风险点列表；输出：回滚步骤文档；验证：演练可在单发布窗口完成）
