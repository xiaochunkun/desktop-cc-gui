# Implementation Tasks: Improve Codex Plan Visibility And Default Mode

## 1. Code 模式下计划可见性修复

- [x] 1.1 梳理 Plan 面板渲染优先级（`hasPlanData` 与 `isPlanMode` 判定顺序）。
- [x] 1.2 修改渲染逻辑：有计划数据时在 Code 模式也展示计划内容。
- [x] 1.3 保留无计划数据时的模式引导提示文案。

## 2. 批量编辑卡片 Plan 快览完整展示

- [x] 2.1 定位批量编辑卡片 Plan 快览容器的高度与 overflow 限制。
- [x] 2.2 改为“可滚动内容区”，保证长计划可完整查看。
- [x] 2.3 验证弹层/卡片在窄屏下不被父容器裁切。

## 3. 右侧 Plan 面板关闭能力

- [x] 3.1 在右侧 Plan 面板标题增加关闭按钮。
- [x] 3.2 接入面板显隐状态管理，关闭后保留计划数据。
- [x] 3.3 复用“查看完整 Plan 面板”入口恢复显示。

## 4. Codex 默认模式改为 Plan

- [x] 4.1 调整 Codex 协作模式默认初始化值为 Plan。
- [x] 4.2 保证用户显式选择与持久化优先级高于默认值。
- [x] 4.3 回归非 Codex 引擎行为不变。

## 5. 测试与验证

- [x] 5.1 新增/更新组件测试：Code 模式已有计划可见。
- [x] 5.2 新增/更新组件测试：批量编辑卡片 Plan 快览不截断。
- [x] 5.3 新增/更新组件测试：Plan 面板可关闭并恢复。
- [x] 5.4 新增/更新逻辑测试：Codex 默认模式为 Plan。
- [x] 5.5 执行 `typecheck`、目标测试用例与手工 UI 验证。

## 6. Codex 实时流与详情合并（仅 Codex）

- [x] 6.1 梳理实时活动标签与 tool 详情卡片的现有数据来源与差异点。
- [x] 6.2 抽取统一摘要规则（command/tool fact + reasoning context）。
- [x] 6.3 将统一摘要规则接入 Codex 幕布实时活动显示。
- [x] 6.4 将同一摘要规则接入对应详情卡片标题/摘要区，减少文案割裂。
- [x] 6.5 增加引擎门控测试，保证该策略仅影响 `activeEngine === "codex"`。
