## 1. 文件树交互语义改造（P0，前置依赖）

- [x] 1.1 将文件树行交互改为“单击选中、双击触发动作”（输入：现有单击打开实现；输出：单击不再打开/展开；验证：单击文件与文件夹均仅更新选中态）。
- [x] 1.2 实现双击文件打开与双击文件夹展开/折叠（依赖：1.1；输入：双击事件；输出：复用既有打开与展开链路；验证：双击行为回归测试通过）。
- [x] 1.3 保持右键菜单、键盘导航、`+` 提及入口不受影响（依赖：1.1,1.2；输入：现有交互链路；输出：无行为回退；验证：`FileTreePanel` 相关用例全绿）。

## 2. 多选模型与拖拽源（P0，依赖 1.x）

- [x] 2.1 引入文件树多选状态（主选中项、锚点、选中集合）并定义清理时机（输入：节点点击事件；输出：稳定可追踪的选择模型；验证：状态单测覆盖）。
- [x] 2.2 实现平台手势映射：macOS `⌘+Click` / Windows `Ctrl+Click` 切换选择，`Shift+Click` 区间选择（依赖：2.1；输入：修饰键点击事件；输出：跨平台一致选择语义；验证：手势场景测试通过）。
- [x] 2.3 文件树拖拽源支持“从已选集合发起时携带多路径”，并对同批 payload 去重（依赖：2.1,2.2；输入：dragstart 事件；输出：可消费的多路径 payload；验证：drag payload 断言通过）。

## 3. Composer 批量路径注入（P0，依赖 2.x）

- [x] 3.1 抽取路径列表注入共享函数，统一 `window.handleFilePathFromJava` 与 drop 路径处理（输入：路径数组；输出：一致 `@path` 注入与 tag 渲染链路；验证：两入口结果一致）。
- [x] 3.2 扩展 `usePasteAndDrop` 支持批量文件/文件夹路径注入，并保持拖拽边界仅在 Composer 输入区生效（依赖：3.1；输入：DOM drop payload；输出：批量引用可见且非目标区不注入；验证：hook 边界与批量用例通过）。
- [x] 3.3 接入窗口级外部拖拽路径并与图片附件链路隔离（依赖：3.1,3.2；输入：Tauri `onDragDropEvent.paths` 与图片 drop；输出：路径走引用、图片走附件；验证：互斥场景测试通过）。

## 4. Win/mac 兼容保护（P0，依赖 3.x）

- [x] 4.1 增加路径归一化策略（分隔符、Windows 盘符大小写）并统一去重键（输入：跨平台路径样例；输出：稳定匹配与插入；验证：路径归一化单测通过）。
- [x] 4.2 补充 Finder 外部拖拽路径与 Windows 风格路径回归场景（依赖：4.1；输入：mac/Win 典型路径；输出：均可成功注入；验证：平台样例测试通过）。

## 5. 回归与验收（P1，收尾）

- [x] 5.1 执行文件树与 Composer 受影响测试集（依赖：1.x,2.x,3.x,4.x；输入：目标测试范围；输出：全部通过；验证：`pnpm vitest` 目标用例全绿）。
- [x] 5.2 执行类型检查与基础构建验证（依赖：5.1；输入：前端工程；输出：无新增 type error；验证：`pnpm tsc --noEmit` 通过）。
- [x] 5.3 完成交互矩阵手工验收（单击选中、双击打开/展开、多选拖拽、外部拖拽、`+` 链路回归）（依赖：5.1,5.2；输入：关键链路清单；输出：验收记录与异常说明；验证：变更备注可追踪）。

## 6. 本轮实现备注（2026-03-18）

- [x] 6.1 修复内部拖拽与外部拖拽事件时序冲突（`document dragend` 提前清理 bridge 导致内部路径丢失）。
- [x] 6.2 增强拖拽命中稳健性：文档级捕获 + `dragend` 兜底复用 `+` 号链路。
- [x] 6.3 补齐拖拽中的输入区遮罩显示，确保内部/外部拖拽均有视觉反馈。
