## 1. Markdown 模式状态收束

- [x] 1.1 调整 `FileViewPanel` 的 Markdown 默认进入逻辑（优先级: P0，依赖: 无；输入: 当前 `mode` / `mdViewMode` 状态机；输出: `*.md` / `*.mdx` 首次打开默认进入单屏源码编辑；验证: 打开 Markdown 文件时不再出现双栏布局）
- [x] 1.2 收敛 Markdown 顶部操作按钮为“编辑 / 预览”主切换（优先级: P0，依赖: 1.1；输入: 当前 Markdown 顶部按钮与双层模式切换；输出: 单一主切换路径；验证: 从编辑切到预览、再切回编辑时内容与焦点行为正确）
- [x] 1.3 移除或替换 Markdown 默认双栏编辑渲染分支（优先级: P0，依赖: 1.1；输入: 当前 split-pane Markdown edit 分支；输出: 单屏编辑渲染；验证: Markdown 编辑态仅渲染一个编辑面板）

## 2. 文本视图主题与可读性

- [x] 2.1 为文件查看器定义主题感知的正文对比度基线（优先级: P0，依赖: 无；输入: `file-view-panel.css` 中现有 preview / editor 颜色与 surface 变量；输出: 浅色主题浅底深字、深色主题深底浅字；验证: 切换 light / dark 主题后文本主体不再呈现低对比灰字）
- [x] 2.2 让 CodeMirror 编辑区域跟随文件查看器主题语义（优先级: P0，依赖: 2.1；输入: 当前固定 `theme=\"dark\"` 与局部样式；输出: 编辑态与预览态共享一致可读性基线；验证: 浅色主题下编辑器不再保持割裂的深色视觉）
- [x] 2.3 校准行号、活动行、选区与基础 token 颜色层级（优先级: P1，依赖: 2.1, 2.2；输入: 现有 gutter / active line / token 颜色；输出: 与新背景契合的辅助视觉层级；验证: 常见文本与代码文件在两套主题下均可清晰阅读）

## 3. no-wrap 与 Markdown 宽内容渲染

- [x] 3.1 关闭文件查看器源码视图的自动换行并补齐横向滚动（优先级: P0，依赖: 2.1；输入: 当前代码预览与 CodeMirror scroller 样式；输出: 预览态与编辑态默认 no-wrap；验证: 超长单行内容可通过横向滚动完整查看）
- [x] 3.2 优化 Markdown 文件查看器容器的宽内容渲染规则（优先级: P0，依赖: 1.1, 3.1；输入: `Markdown` 组件与 `messages.css` / `file-view-panel.css` 中现有宽内容样式；输出: 代码块、表格、长链接等宽结构不被强制折行破坏；验证: 宽 Markdown 结构可横向访问且不出现不可达裁切）
- [x] 3.3 将文件查看器的 Markdown 样式覆盖收敛到 `fvp-` 命名空间（优先级: P1，依赖: 3.2；输入: 共享 Markdown 样式规则；输出: 文件查看器局部覆盖不污染消息区；验证: 消息区 Markdown 视觉与交互不发生非预期变化）

## 4. 回归验证与测试

- [x] 4.1 为 Markdown 默认模式与主切换补充前端测试（优先级: P0，依赖: 1.1, 1.2, 1.3；输入: 现有文件查看器测试基线；输出: 覆盖默认进入编辑态和编辑/预览切换的测试；验证: 相关测试稳定通过）
- [x] 4.2 为文本主题与 no-wrap 行为补充验证用例（优先级: P1，依赖: 2.1, 2.2, 3.1；输入: 现有文件查看器渲染测试或手工验证清单；输出: 覆盖 light / dark 对比度与长行横向滚动的验证；验证: 长行文件和普通文本文件均符合新契约）
- [x] 4.3 执行最小回归检查并记录结果（优先级: P0，依赖: 4.1, 4.2；输入: 本次变更影响的文件查看器路径；输出: 至少包含 Markdown、普通文本、代码文件三类的验证结果；验证: 文件树多 Tab、文件打开/保存、Markdown 预览均无新增回归）
