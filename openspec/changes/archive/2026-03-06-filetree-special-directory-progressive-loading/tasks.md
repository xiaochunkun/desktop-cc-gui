## 1. 特殊目录判定基线（P0）

- [x] 1.1 (P0, depends: none, <=2h) 在 `src-tauri/src/workspaces/files.rs` 新增特殊目录分类函数（dependency/build_artifact）；输入=目录名与相对路径；输出=稳定 `specialKind` 判定；验证=Rust 单测覆盖命中与未命中目录。
- [x] 1.2 (P0, depends: 1.1, <=2h) 将提案中的特殊目录全集固化到白名单并支持 `cmake-build-*` 模式；输入=目录白名单；输出=规则常量与匹配函数；验证=表驱动测试覆盖典型目录（含 `.m2/.ivy2/.cargo`）。

## 2. 后端渐进加载接口（P0）

- [x] 2.1 (P0, depends: 1.2, <=2h) 新增目录子级查询 command（单层 children 返回）；输入=workspaceId + directoryPath；输出=子目录/子文件 + gitignored 标记；验证=command 单测与错误码断言通过。
- [x] 2.2 (P0, depends: 2.1, <=2h) 在初始 `list_workspace_files` 中对特殊目录仅返回节点本身、屏蔽深层后代；输入=扫描结果；输出=首屏 payload 裁剪；验证=回归测试确认普通目录行为不变。
- [x] 2.3 (P0, depends: 2.1, <=2h) 增加路径安全校验（拒绝 `..`/越界路径/无效绝对路径）；输入=恶意路径样本；输出=可恢复错误响应；验证=安全边界测试通过且无 panic。

## 3. 前端文件树按需展开（P0）

- [x] 3.1 (P0, depends: 2.1, <=2h) 扩展文件树节点状态（unknown/loading/ready/error）并仅在特殊目录启用；输入=初始树节点；输出=可追踪 childrenState；验证=TypeScript 类型检查通过。
- [x] 3.2 (P0, depends: 3.1,2.1, <=2h) 接入展开时按目录拉取 children（单飞去重 + 缓存复用）；输入=目录展开事件；输出=逐级加载行为；验证=Vitest 覆盖首次展开请求、重复展开不重复请求。
- [x] 3.3 (P0, depends: 3.2, <=2h) 加入节点级错误态与重试操作；输入=后端失败响应；输出=可恢复 UI 提示；验证=Vitest 断言失败后可重试且树仍可操作。
- [x] 3.4 (P0, depends: 3.2, <=2h) 补充“特殊子树递归懒加载”用例：子目录名未命中特殊白名单也能继续按层加载；输入=如 `node_modules/@scope/pkg`；输出=二级/三级展开继续触发子级查询；验证=Vitest 覆盖多层展开链路。

## 4. 非回归保护：引用组件行为不变（P0）

- [x] 4.1 (P0, depends: 3.2, <=2h) 回归验证文件打开、多 Tab、@ 引用、diff 打开链路未改变；输入=现有交互测试；输出=非回归断言；验证=目标测试套件全绿。
- [x] 4.2 (P0, depends: 4.1, <=2h) 确认普通目录仍沿用当前全量行为且搜索/展开体验不漂移；输入=普通目录场景样本；输出=行为一致性报告；验证=手测清单 + 自动化断言一致。

## 5. 质量门与发布守卫（P1）

- [x] 5.1 (P1, depends: 2.3,3.3,4.2, <=2h) 执行 Rust/TS 最小质量门（targeted tests + typecheck）；输入=全部改动；输出=测试与类型检查结果；验证=命令通过且无新增阻断错误。
- [x] 5.2 (P1, depends: 5.1, <=2h) 记录性能对比（至少 `node_modules` 大仓库首屏加载时间/内存峰值）；输入=变更前后采样；输出=性能对比记录；验证=结果显示峰值下降或卡顿时间缩短。
