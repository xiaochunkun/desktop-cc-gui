## 1. Radar 历史删除契约与数据通路（P0，前置）

- [x] 1.1 定义 Radar 历史删除输入输出契约（输入：单条/批量记录 ID；输出：successIds/failedItems）；验证：契约类型与错误分类单测通过。
- [x] 1.2 实现删除结果驱动的 recent feed 同步（依赖：1.1；输入：删除结果；输出：管理视图与 Radar 概览一致数据源；验证：删除后无幽灵回显）。
- [x] 1.3 增加删除请求防重入守卫（依赖：1.1；输入：删除执行状态；输出：重复提交被阻断；验证：并发点击测试通过）。

## 2. 设置页“雷达历史管理”入口与管理视图（P0，依赖 1.x）

- [x] 2.1 在“其他设置”新增“雷达历史管理”入口（输入：现有设置导航结构；输出：单层可达入口；验证：UI 路径可达性测试通过）。
- [x] 2.2 实现管理视图列表渲染（依赖：1.2；输入：recent completed 数据；输出：可删除列表 + 稳定标识；验证：列表渲染与空态测试通过）。
- [x] 2.3 实现单条删除交互（依赖：2.2；输入：单条记录删除动作；输出：成功移除 + 反馈提示；验证：单条删除测试通过）。

## 3. 连招删除状态机（P0，依赖 2.x）

- [x] 3.1 实现批量选择与确认态（输入：用户多选行为；输出：confirming 状态与待删集合；验证：确认弹层与计数正确）。
- [x] 3.2 实现部分失败保留与同页重试（依赖：3.1；输入：部分失败返回；输出：失败项保留选中、可二次删除；验证：连招重试场景测试通过）。
- [x] 3.3 实现执行态禁用与完成态恢复（依赖：3.1；输入：deleting/settled 状态切换；输出：删除期间控件禁用、完成后可继续下一轮；验证：防重入与连续两轮删除测试通过）。

## 4. 回归与发布门禁（P0，收尾）

- [x] 4.1 补齐规格对齐测试（输入：spec scenarios；输出：单条成功、批量部分失败、连招重试、跨视图一致性测试；验证：目标测试套件全绿）。
- [x] 4.2 运行类型检查与构建自检（依赖：4.1；输入：前端/后端改动；输出：无新增类型错误；验证：`pnpm tsc --noEmit`、`pnpm run build` 通过）。
- [x] 4.3 预留回滚开关验证（依赖：2.x/3.x；输入：feature flag 开关；输出：关闭后隐藏管理入口且不影响 Radar 只读功能；验证：手工回归通过）。
