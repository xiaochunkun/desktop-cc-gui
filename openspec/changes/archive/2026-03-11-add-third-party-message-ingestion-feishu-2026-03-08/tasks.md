## Phase 1: 飞书接入配置与可视化（P0）

- [x] 1.1 [P0][依赖: 无] 定义统一 Provider 配置类型与状态枚举  
  输入: 需求文档中的状态集合（未配置/已配置/连接中/在线/失败）  
  输出: TS/Rust 对齐的类型定义  
  验证: typecheck 通过，前后端状态映射单测通过

- [x] 1.2 [P0][依赖: 1.1] 新建“第三方消息接入”页面壳与导航入口  
  输入: 现有设置/导航框架  
  输出: 可访问页面与空态卡片  
  验证: UI 测试覆盖入口可见性，路由可达

- [x] 1.3 [P0][依赖: 1.1] 实现 Feishu 配置表单（App ID/App Secret）  
  输入: Provider 配置 schema  
  输出: 表单校验、保存动作、密钥脱敏显示  
  验证: 表单校验测试、敏感字段不回显测试

- [x] 1.4 [P0][依赖: 1.3] 新增后端配置存储命令并加密保存 Secret  
  输入: 表单提交参数  
  输出: `save_provider_config/get_provider_config` IPC  
  验证: Rust 单测验证加密落盘与读取

- [x] 1.5 [P0][依赖: 1.4] 实现 Feishu 长连接启动/停止/状态查询命令  
  输入: 已保存配置  
  输出: `start_provider_connection/stop_provider_connection/get_provider_status`  
  验证: 集成测试覆盖成功与失败路径

- [x] 1.6 [P0][依赖: 1.5] 实现“连通性测试”按钮与结果面板  
  输入: Provider 运行状态与错误码  
  输出: 成功/失败诊断 UI（含建议动作）  
  验证: 组件测试断言不同状态文案

- [x] 1.7 [P0][依赖: 1.5] 接入 `im.message.receive_v1` 到统一消息模型  
  输入: Feishu 事件 payload  
  输出: `ExternalMessage` 入站记录  
  验证: 事件映射测试、message_id 去重测试

- [x] 1.8 [P0][依赖: 1.7] 实现入站消息列表 UI（最近消息可见）  
  输入: `ExternalMessage` 列表数据  
  输出: 时间线 + 明细弹层  
  验证: UI 测试覆盖渲染、排序、空态

- [x] 1.9 [P1][依赖: 1.8] 增加连接状态与消息接收指标埋点  
  输入: 连接生命周期事件  
  输出: 结构化日志与前端状态指标  
  验证: 日志字段断言测试

- [x] 1.10 [P0][依赖: 1.2~1.8] Phase 1 回归与验收  
  输入: 完整接入链路  
  输出: 验证记录（手测 + 自动化）  
  验证: `pnpm vitest ...` + `pnpm tsc --noEmit` + 联调截图

## Phase 2: 特殊 Session 与任务执行（P1）

- [x] 2.1 [P1][依赖: 1.7] 设计 `ExternalSession` 数据模型与状态机  
  输入: 外部消息模型、线程模型  
  输出: session schema 与状态流转图  
  验证: 状态机单测覆盖合法/非法迁移

- [x] 2.2 [P1][依赖: 2.1] 从入站消息创建特殊 session 的 UI 入口  
  输入: 入站消息项  
  输出: “创建特殊 session”动作与会话详情页  
  验证: UI 测试覆盖创建与跳转

- [x] 2.3 [P1][依赖: 2.1] 实现飞书回复动作（Reply Action）  
  输入: session_id + 回复文本  
  输出: `reply_to_external_message` IPC 与结果回执  
  验证: 接口测试覆盖成功/失败分支

- [x] 2.4 [P0][依赖: 2.1] 实现本地任务动作并加“显式确认”守卫  
  输入: 任务请求（命令/参数）  
  输出: 审批弹窗 + 执行结果  
  验证: 未确认不可执行、确认后可执行

- [x] 2.5 [P1][依赖: 2.3,2.4] 记录特殊 session 审计链路  
  输入: 回复/任务动作事件  
  输出: 可追踪审计记录（消息ID、sessionID、执行结果）  
  验证: 审计查询测试、字段完整性断言

- [x] 2.6 [P1][依赖: 2.2~2.5] Feature Flag 灰度与 Phase 2 验收  
  输入: `external_special_session_v1` flag  
  输出: 灰度发布清单与回滚开关  
  验证: 开关前后行为差异测试
